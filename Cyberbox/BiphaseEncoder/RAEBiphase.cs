using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// This is going to be the worst one to convert as its not gonna be a simple, "lets take the bits, video, and audio and push them to an mp4." It will have to be either one 2 channel .wav, or 2, 1 channel .wav's.
// FML
// update: gonna make it 2 1 channel wavs, not only will it make the encoder faster and more lightweight in that regard but it will allow you to throw the
// 2 seperate tracks into a multitracking daw like protools or reaper so you can use the daw to record them to 4 track tape (i.e. teac a3440, or tascam 234 syncaset just to name a couple)

public class RAEBiphase : MonoBehaviour
{
    // Start is called before the first frame update
    public void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
