using System.Collections;
using System.Collections.Generic;

// This should be straight forward, I just take what I did for PTTCybers and swap a couple things (She said unconfidently)

public class ModernCybers
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
